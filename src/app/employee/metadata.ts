import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: {
    default: 'Employee Portal',
    template: '%s | weCargo Employee'
  },
  description: 'weCargo employee portal for managing deliveries and orders'
}; 